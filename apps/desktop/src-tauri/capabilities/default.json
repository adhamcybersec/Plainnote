{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default-deny baseline. Permissions are added explicitly per milestone — no broad allowlists.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
